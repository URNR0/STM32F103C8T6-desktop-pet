# AGENTS.md

Use this file for workspace-level operating rules.

## Working Rules

- **只修改文件，不执行 `git commit`，不执行 `git push`。** 版本管理（commit/push）由用户自己完成。
- 保持改动小而可逆，紧扣用户请求。
- 高优先级系统/运行时/安全/用户指令优先于本文件。